using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Movement_Mapping : MonoBehaviour
{
    //If you want to remap the keys, simply change the string value here (using external script of course). Key names: https://docs.unity3d.com/Manual/class-InputManager.html ~ TLF
    public string move_UP = "w", move_LEFT = "a", move_RIGHT = "d", move_DOWN = "s", jump = "space", run = "left shift"; 
}
